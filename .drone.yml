---
# ==============================
# COMMON
# ==============================
kind: pipeline
type: exec
name: deploy

platform:
  os: linux
  arch: amd64

steps:
- name: deploy prod
  environment:
    DEPLOY_PROD_CMD:
      from_secret: deploy_prod_cmd
  commands:
  - VERSION=$(./ci/version.sh "date -d @")
  - eval "${DEPLOY_PROD_CMD//VERSION/$VERSION}"

trigger:
  event:
  - promote

---
kind: pipeline
type: exec
name: load testing

platform:
  os: linux
  arch: amd64

steps:
- name: exit if no load
  commands:
  - if [ "$DRONE_BUILD_EVENT" != "cron" ] && [ "$act" != "load" ]; then exit 78; fi

- name: start staging
  environment:
    START_STAGING_CMD:
      from_secret: start_staging_cmd
  commands:
  - eval "${START_STAGING_CMD//VERSION/$VERSION}"

- name: test
  environment:
    DOMAIN:
      from_secret: staging_domain
  commands:
  - ./ci/test-load.sh $DOMAIN

- name: copy results
  environment:
    COPY_LOAD_TEST_RESULTS_CMD:
      from_secret: copy_load_test_results_cmd
  commands:
  - eval $COPY_LOAD_TEST_RESULTS_CMD
  when:
    status:
    - success
    - failure

trigger:
  event:
  - custom
  - cron

# ==============================
# CUSTOM
# ==============================

---
kind: pipeline
type: exec
name: (custom) build & test

platform:
  os: linux
  arch: amd64

steps:
- name: exit if load
  commands:
  - if [ "$act" = "load" ]; then exit 78; fi

- name: docker login
  environment:
    USERNAME:
      from_secret: docker_user
    PASSWORD:
      from_secret: docker_pass
  commands:
  - docker login -u $USERNAME -p $PASSWORD

- name: build
  commands:
  - git remote set-url origin git@github.com:gerrkoff/dark-deeds.git
  - ./ci/build.sh push "date -d @"

- name: docker prune
  commands:
  - docker system prune -f

- name: start staging
  environment:
    START_STAGING_CMD:
      from_secret: start_staging_cmd
  commands:
  - VERSION=$(./ci/version.sh "date -d @")
  - eval "${START_STAGING_CMD//VERSION/$VERSION}"

- name: test
  environment:
    URL:
      from_secret: staging_url
  commands:
  - ./ci/test-e2e.sh $URL staging

trigger:
  event:
  - custom

---
kind: pipeline
type: docker
name: (custom) notify

steps:
- name: telegram
  image: appleboy/drone-telegram
  settings:
    token:
      from_secret: telegram_bot_token
    to:
      from_secret: telegram_user_id

depends_on:
- (custom) build & test
- deploy
- load testing

trigger:
  status:
  - success
  - failure
  event:
  - promote
  - custom
  - cron

# # ==============================
# # STAGING – FULL COPY OF CUSTOM WITH ADJUSTED TRIGGER
# # ==============================

---
kind: pipeline
type: exec
name: (staging) build & test

platform:
  os: linux
  arch: amd64

steps:
- name: exit if load
  commands:
  - if [ "$act" = "load" ]; then exit 78; fi

- name: docker login
  environment:
    USERNAME:
      from_secret: docker_user
    PASSWORD:
      from_secret: docker_pass
  commands:
  - docker login -u $USERNAME -p $PASSWORD

- name: build
  commands:
  - git remote set-url origin git@github.com:gerrkoff/dark-deeds.git
  - ./ci/build.sh push "date -d @"

- name: docker prune
  commands:
  - docker system prune -f

- name: start staging
  environment:
    START_STAGING_CMD:
      from_secret: start_staging_cmd
  commands:
  - VERSION=$(./ci/version.sh "date -d @")
  - eval "${START_STAGING_CMD//VERSION/$VERSION}"

- name: test
  environment:
    URL:
      from_secret: staging_url
  commands:
  - ./ci/test-e2e.sh $URL staging

trigger:
  branch:
  - staging
  event:
  - push

---
kind: pipeline
type: docker
name: (staging) notify

steps:
- name: telegram
  image: appleboy/drone-telegram
  settings:
    token:
      from_secret: telegram_bot_token
    to:
      from_secret: telegram_user_id

depends_on:
- (staging) build & test

trigger:
  status:
  - success
  - failure
  branch:
  - staging
  event:
  - push
