export function isKeyEsc(e: React.KeyboardEvent | KeyboardEvent): boolean {
    return e.key === 'Escape'
}

export function isKeyEnter(e: React.KeyboardEvent | KeyboardEvent): boolean {
    return e.key === 'Enter'
}
